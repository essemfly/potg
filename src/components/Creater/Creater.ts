export type Creater = {
  id: number;
  name: string;
  email: string;
  logoUrl: string;
  twitchUrl: string;
  avatarUrl: string;
};

export type Player = {
  id: number;
  name: string;
  email: string;
  twitchUrl: string;
  avatarUrl: string;
};
